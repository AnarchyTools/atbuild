(package
 :name "warnings"
 :poland "invalid" ;; we can't currently warn for this, but we should someday

     :tasks {
        :default {
            :tool "atllbuild"
            :output-type "static-library"
            :name "test"
            :germany "invalid"
            :sources ["Foo.swift"]
        }
     }
)